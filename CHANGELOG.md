# Changelog

## Unreleased

- Field-hardened `graph-method` with nine receipt-backed sharpenings: checked-by-default
  verification, completion-route enumeration, observer budgets, payer attribution, concrete
  how-blocks, trigger-shape fixtures, bounded continuation, `SUCCEEDED` stage predicates, and
  explicit release acts.
